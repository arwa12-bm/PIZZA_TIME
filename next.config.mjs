/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.commande-pizzatime.fr','recette-catalogue.aureskonnect.com','pizzatime.fr'],
    }
};

export default nextConfig;

